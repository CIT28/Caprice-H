dump tables
Try to delete the artist
Why the error happened was that the created.artist_id still points to this artist
 remove links from created, then delete from artists
make sure artist gone and no leftover links
Show schema
CREATE TABLE collections (
  id               INTEGER PRIMARY KEY,
  title            TEXT NOT NULL,
  accession_number TEXT NOT NULL UNIQUE,
  acquired         TEXT
);
