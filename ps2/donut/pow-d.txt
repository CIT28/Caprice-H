creating donuts and ingredients tables from schema.sql
verify tables
donut_ingredients  donuts             ingredients      
showing schema for donuts and ingredients
CREATE TABLE IF NOT EXISTS "ingredients" (
    "id" INTEGER PRIMARY KEY,
    "name" TEXT NOT NULL UNIQUE,
    "unit" TEXT NOT NULL,
    "price_per_unit" NUMERIC NOT NULL CHECK ("price_per_unit" > 0)
);
CREATE TABLE IF NOT EXISTS "donuts" (
    "id" INTEGER PRIMARY KEY,
    "name" TEXT NOT NULL UNIQUE,
    -- 0 = not glutenfree, 1 = glutenfree
    "gluten_free" INTEGER NOT NULL CHECK ("gluten_free" IN (0, 1)),
    "price" NUMERIC NOT NULL CHECK ("price" > 0)
);
CREATE TABLE IF NOT EXISTS "donut_ingredients" (
    "id" INTEGER PRIMARY KEY,
    "donut_id" INTEGER NOT NULL,
    "ingredient_id" INTEGER NOT NULL,
    FOREIGN KEY ("donut_id") REFERENCES "donuts"("id"),
    FOREIGN KEY ("ingredient_id") REFERENCES "ingredients"("id")
);
